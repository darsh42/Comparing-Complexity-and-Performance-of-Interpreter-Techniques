**Possible options. explain pros and cons to each option.**

**Choose. explain why.**

Choice: MIPS I

simple, easy to implement. 

less pressure on instruction handling. 
The main benefit to a JIT is the native code generation feature.
An instruction set which requires alot of processing to implement,
i.e. has many non-supported features in the host ISA, will nessasarily
cause JIT compilers to excel. The study is'nt questioning the effactiousness
of JIT compilation in "execute" heavy architectures. It is more looking at 
cases where it isn't so clear cut. I.e. in execute heavy ISAs there are 
clearer reasons to implement JIT compilers.
